# .github/workflows/daily-deploy.yml
name: Daily Schedule Deploy

on:
  schedule:
    # 毎日日本時間の午前9時に実行 (UTC 0:00)
    - cron: '0 0 * * *'
  workflow_dispatch: # 手動で実行したい時用

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # ここでGitHub Pagesなどにデプロイする設定を書く
      # すでに自動デプロイ設定がある場合は、この「schedule」を追加するだけでOK